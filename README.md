UID Verification Telegram Bot
Deploy as a Render Background Worker using Docker.
Environment variables: BOT_TOKEN, ADMIN_CHAT_ID, HEADLESS=true, POLL_SECONDS=2.
Commands: /setlogin NUMBER PASSWORD, /login, /info UID, /screenshot, /status, /stop.
Read-only record lookup; no betting/staking actions.
